export const initialState = {
  items: [],
  filter: '',
};

// const storage = JSON.parse(localStorage.getItem('contacts'));

// export const initialState = {
//   items: storage ?? [],
//   filter: '',
// };
